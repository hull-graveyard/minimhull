{{!
  `loggedIn` is a helper that returns false if no user is connected to the app.
  Otherwise, it returrns the user identities

  `me` is always available in the templates. It describes the current user, if a user is connected
}}
{{#if loggedIn}}
Hullo, {{ me.name }}
{{else}}
Hullo, world!
{{/if}}

